# Default agent configuration.
#
# Usage:
#   harness up                         # uses profiles/agent-default.yaml
#   harness up --agent research        # uses profiles/agent-research.yaml

name: agent
entrypoint: claude
tty: true

providers:
  - profile: github
  - profile: vertex-local
  - profile: atlassian
    env:
      JIRA_URL: ${JIRA_URL}
      JIRA_USERNAME: ${JIRA_USERNAME}
  - profile: gws

# Non-secret env vars injected into sandbox.
# Proxy config: gateway routes inference through its local proxy.
env:
  ANTHROPIC_BASE_URL: https://inference.local
  ANTHROPIC_API_KEY: sk-ant-openshell-proxy-managed
  CLAUDE_CODE_DISABLE_EXPERIMENTAL_BETAS: "1"
