# Groq agent -- tests API key bearer auth via provider profile.
# Tests: custom provider profile registration, bearer token credential style.
# Requires: GROQ_API_KEY env var.
name: test-groq
entrypoint: bash
providers:
  - profile: groq
