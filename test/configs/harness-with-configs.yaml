---
kind: agent
name: test-configs
entrypoint: bash
providers: []
---
kind: config
name: claude.json
content: |
  {"mcpServers": {"test": {"command": "echo"}}}
---
kind: config
name: CLAUDE.md
content: |
  # Test sandbox
  You are in a test environment.
---
kind: config
name: settings.json
content: |
  {"permissions": {"allow": ["Bash(*)"]}}
