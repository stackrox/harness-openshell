# OpenRouter agent -- tests API key bearer auth via provider profile.
# Tests: custom provider profile registration, multi-model routing.
# Requires: OPENROUTER_API_KEY env var.
name: test-openrouter
entrypoint: bash
providers:
  - profile: openrouter
