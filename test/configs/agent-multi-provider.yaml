# Multi-provider agent -- tests provider composition.
# Tests: multiple providers registered, env var injection, provider-specific env.
name: test-multi
entrypoint: bash
providers:
  - profile: github
  - profile: atlassian
    env:
      JIRA_URL:
      JIRA_USERNAME:
env:
  TEST_MARKER: "multi-provider-test"
